<section>
    <h1>Register</h1>
    <main>
        <form class="aligned" action="/auth/register" method="post"> {{!-- send data to authController.js -> authController.post('/register', async (req, res) => { --}}
            {{#if error}} {{!-- If we have wrong info in the Form, an error message will be shown. The error that is in the if comes from the createController -> router.post( -> catch as context --}}
                {{!-- 1* v1 - all fields are required --}}
                {{!-- v2 - array of errors --}}
                <div class="error-box">
                    {{#each error}} {{!-- We get out error messages as an Array of stings, this is why we have to render each error separately (createController.js -> router.post( -> catch -> res.render( -> error:)  --}}
                        <p>{{this}}</p> {{!-- this, instead of error - is used when we have #each on an Array of strings --}}
                    {{/each}}
                </div>
            {{/if}}

            <div class="aligned">
                <label><span>Username</span><input type="text" name="username"></label> {{!-- the <span> is because of the css --}}
                <label><span>Password</span><input type="password" name="password"></label>
                <label><span>Repeat Password</span><input type="password" name="repass"></label>
            </div>
            
            <input class="cta action" type="submit" value="Register">
        </form> 
                    <footer>Already have an account? <a href="/auth/login">Login here</a></footer>
    </main>
</section>

